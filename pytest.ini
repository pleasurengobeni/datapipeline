[pytest]
testpaths = web_ui/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Make the repo root importable so 'import web_ui.app' resolves
pythonpath = .
markers =
    live: marks tests that require a live database connection (etl_test_db)
