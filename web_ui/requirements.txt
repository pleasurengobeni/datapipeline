flask==3.0.3
werkzeug==3.0.6
sqlalchemy==1.4.52
psycopg2-binary==2.9.9
pandas>=1.5.0
pyarrow>=12.0.0
cryptography>=43.0.0
# pytest is a dev tool — do not install in production images
# If you need it in CI, add it to a separate dev-requirements.txt
